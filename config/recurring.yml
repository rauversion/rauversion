development:
  service_bookings_lifecycle_sweep:
    class: ServiceBookings::LifecycleSweepJob
    queue: default
    schedule: every 15 minutes

production:
  service_bookings_lifecycle_sweep:
    class: ServiceBookings::LifecycleSweepJob
    queue: default
    schedule: every 15 minutes
